import { Routes } from '@angular/router';

// Portafolio de una sola página: las rutas quedan listas para crecer
// (ej. /proyectos/:id en el futuro) sin romper la estructura actual.
export const routes: Routes = [];
